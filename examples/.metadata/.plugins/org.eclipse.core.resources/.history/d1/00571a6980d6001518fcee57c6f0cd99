package family

import 'flowchart.melange'

languageBinding {
	bind( RequiredStateMachines, ProvidedConstraints  )
	bind( RequiredStateMachines, ProvidedConstraints  )
	bind( StateMachinesMT, ImperativeLanguage  )
	bind( RequiredConstraints, ProvidedExpressions  )
}